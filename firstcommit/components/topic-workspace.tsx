"use client";
import Link from "next/link";
const tools=[["Smart Notes","/notes","Design revision notes, flashcards, and memory hooks."],["Diagram Studio","/diagrams","Generate a flowchart or UML explanation."],["AI Interview","/interview","Explain the concept aloud and get feedback."],["Concept X-Ray","/concept-xray","Find the first incorrect assumption."],["Learning Canvas","/canvas","Draw the algorithm, table, or reasoning."],["Live Class","/live","Discuss this focused topic with the class."]] as const;
export function TopicWorkspace({topic,sources}:{topic:string;sources:{title:string}[]}){const clean=topic.trim().slice(0,300);const query=new URLSearchParams({topic:clean}).toString();return <section className="topic-workspace"><header><span className="eyebrow">TOPIC WORKSPACE</span><h2>Keep learning “{clean}” your way.</h2><p>ShikshaMesh carries this topic into every learning tool. AI tools still retrieve only from your authorized class notes.</p>{sources.length>0&&<small>Retrieved from: {sources.map(source=>source.title).filter((title,index,all)=>all.indexOf(title)===index).join(", ")}</small>}</header><div className="topic-tool-grid">{tools.map(([title,path,description])=><Link key={path} href={`${path}?${query}`}><b>{title}</b><span>{description}</span><em>Open →</em></Link>)}</div></section>;}
