import { afterEach, describe, expect, it } from "vitest";
import { deploymentSafety } from "../lib/deployment-safety";

const original={ environment:process.env.DEPLOYMENT_ENV, demo:process.env.ALLOW_DEMO_IDENTITY, debug:process.env.DEBUG_LOGS, pool:process.env.COGNITO_USER_POOL_ID, client:process.env.COGNITO_APP_CLIENT_ID, db:process.env.DATABASE_URL, secret:process.env.SIGNUP_ROLE_TOKEN_SECRET, origin:process.env.REALTIME_ALLOWED_ORIGIN };
afterEach(()=>{process.env.DEPLOYMENT_ENV=original.environment;process.env.ALLOW_DEMO_IDENTITY=original.demo;process.env.DEBUG_LOGS=original.debug;process.env.COGNITO_USER_POOL_ID=original.pool;process.env.COGNITO_APP_CLIENT_ID=original.client;process.env.DATABASE_URL=original.db;process.env.SIGNUP_ROLE_TOKEN_SECRET=original.secret;process.env.REALTIME_ALLOWED_ORIGIN=original.origin;});
describe("deployment safety",()=>{it("rejects unsafe production configuration",()=>{process.env.DEPLOYMENT_ENV="production";process.env.ALLOW_DEMO_IDENTITY="true";process.env.DEBUG_LOGS="true";process.env.REALTIME_ALLOWED_ORIGIN="http://localhost:3000";expect(deploymentSafety()).toMatchObject({production:true,ready:false});expect(deploymentSafety().issues).toContain("Demo identity must be disabled in production");});it("does not block local development",()=>{process.env.DEPLOYMENT_ENV="development";expect(deploymentSafety()).toEqual({production:false,ready:true,issues:[]});});});
