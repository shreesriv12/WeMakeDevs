"use client";
import { useState } from "react";
import { useSearchParams } from "next/navigation";
import { SignInPanel, useAuthSession } from "@/components/auth-session";
import { MermaidDiagram } from "@/components/mermaid-diagram";
type Kind="flowchart"|"sequence"|"class"|"state"|"er";
const initial=`flowchart TD\n  A[Course notes] --> B[Understand concept]\n  B --> C[Practice]\n  C --> D[Explain it back]`;
export default function DiagramsPage(){const params=useSearchParams();const {idToken,loading,apiFetch}=useAuthSession();const [topic,setTopic]=useState(()=>params.get("topic")??"Binary Lifting"),[kind,setKind]=useState<Kind>("flowchart"),[code,setCode]=useState(initial),[submitted,setSubmitted]=useState(initial),[status,setStatus]=useState("Start from a template or generate from your approved course notes."),[busy,setBusy]=useState(false),[error,setError]=useState("");async function generate(){setBusy(true);setError("");try{const response=await apiFetch("/api/diagrams",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({classId:"cn-b",topic,kind})});const body=await response.json();if(!response.ok)throw new Error(body.error??"Unable to generate diagram");setCode(body.code);setSubmitted(body.code);setStatus(`Generated with ${body.provider} · ${body.sources.length} authorized note source(s)`);}catch(reason){setError(reason instanceof Error?reason.message:"Unable to generate diagram");}finally{setBusy(false);}}if(loading)return <main><p>Checking sign-in session…</p></main>;if(!idToken)return <main><section className="hero"><span className="eyebrow">VISUAL LEARNING</span><h1>Diagram Studio</h1><p>Build and explain flowcharts from your course material.</p></section><SignInPanel/></main>;return <main className="diagram-page"><section className="hero"><span className="eyebrow">MERMAID · NOTE-GROUNDED VISUALS</span><h1>Turn a topic into a diagram.</h1><p>Create flowcharts, UML class and sequence diagrams, state machines, and ER diagrams. Generation only receives your authorized course notes.</p></section><section className="diagram-studio"><aside className="diagram-controls"><label htmlFor="diagram-topic">Topic</label><input id="diagram-topic" value={topic} onChange={event=>setTopic(event.target.value)}/><label htmlFor="diagram-kind">Diagram type</label><select id="diagram-kind" value={kind} onChange={event=>setKind(event.target.value as Kind)}><option value="flowchart">Flowchart</option><option value="sequence">UML sequence diagram</option><option value="class">UML class diagram</option><option value="state">State machine</option><option value="er">Entity relationship diagram</option></select><button type="button" onClick={()=>void generate()} disabled={busy}>{busy?"Generating…":"Generate from notes"}</button><label htmlFor="mermaid-code">Mermaid code</label><textarea id="mermaid-code" rows={18} spellCheck={false} value={code} onChange={event=>setCode(event.target.value)}/><button type="button" className="diagram-preview-button" onClick={()=>{setSubmitted(code);setStatus("Preview updated from your Mermaid code.");}}>Update preview</button><p className="diagram-help">You can edit Mermaid yourself. Example: <code>A --&gt; B</code> creates an arrow.</p></aside><section className="diagram-preview"><div className="diagram-preview-head"><div><span className="eyebrow">LIVE PREVIEW</span><h2>{topic||"Untitled diagram"}</h2></div><button type="button" onClick={()=>navigator.clipboard.writeText(submitted)}>Copy code</button></div><MermaidDiagram code={submitted}/><small>{status}</small>{error&&<p className="error">{error}</p>}</section></section></main>;}
